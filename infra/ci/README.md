# CI Infra Notes

Use this directory for CI support assets as the pipeline evolves, for example:

- reusable shell scripts invoked by workflows
- config fragments/templates
- local CI runner helpers

Phase 0 CI wiring begins with `.github/workflows/ci.yml` placeholder jobs.
