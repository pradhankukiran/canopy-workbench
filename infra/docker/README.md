# Docker Infra Notes

Use this directory for repository-owned Docker assets as they are introduced, such as:

- custom Dockerfiles
- compose overrides
- local bootstrap scripts
- image build metadata

Phase 0 local runtime dependencies are currently defined in the root `docker-compose.yml`.
