# Scheduler Service (`@canopy/scheduler`)

Phase 0 placeholder for the future scheduling/orchestration service.

## Future role

- Trigger recurring or time-based workflows.
- Scheduling backend and persistence are intentionally deferred.

## Current state

- Placeholder package metadata, TypeScript config, and source entry.
- Scripts are stubs for early workspace command wiring.

## Commands (from repo root)

- `pnpm --filter @canopy/scheduler dev`
- `pnpm --filter @canopy/scheduler build`
- `pnpm --filter @canopy/scheduler typecheck`
